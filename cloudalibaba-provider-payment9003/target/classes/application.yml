server:
  port: 9003
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      #服务发现地址
      discovery:
        server-addr: localhost:8848

#把后面要监控的东西暴露出来
management:
  endpoints:
    web:
      exposure:
        include: '*'
